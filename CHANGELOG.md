# 更新日志（Changelog）

版本号格式遵循 vMAJOR.MINOR；当前版本：**v1.01**。

## [v1.01] - 2026-08-21（当前）

程序重命名与界面重做。主程序更名为 **EndfieldComboHUD**，界面全新三页布局（干员 / 标识 / 外观）。

### 新增
- **全新三页界面**：
  - 「干员」：分辨率选择、干员显示（1号位~4号位）、操作布局（键盘鼠标 / 手柄 Beta）、显示选项（仅在战斗中显示、隐藏 HUD）
  - 「标识」：抓取头像、恢复默认样式
  - 「外观」：通用设置（标志大小 / 间距 / 不透明度、CD 显示预计秒数、不可用状态）、工具（调试信息 / 恢复默认 / 预览 HUD）、关于
- **分辨率选择**：以分辨率列表替代原"游戏显示器"选择，跨分辨率坐标自动缩放
- **干员显示**：监测槽位文案改为"1号位 ~ 4号位"
- **自动切换样式**：点击"抓取头像"成功后自动切换为角色头像样式（FF14 式半透明 CD 遮罩）
- **新设置路径**：`%APPDATA%\EndfieldComboHUD\`（干净启动，不迁移旧设置）
- 新 exe 名称：`EndfieldComboHUD_v1.01.exe`

### 变更
- **实心圆固定为默认样式**：配色与高亮动画固定不可更改；**不显示干员编号**（与 CD 预计秒数显示冲突，已移除）
- 去除标识样式手动选择下拉（样式由"抓取头像 / 恢复默认样式"动作自动决定）
- **HUD 背景固定透明**（background 不透明度固定为 0），移除对应滑块与设置
- **移除全部提示功能**：提示音、不同槽位音高、首次 READY 静默、"提示"页整体删除
- 槽位颜色自定义移除（仅保留默认配色）
- 位置预设与"调整位置"按钮移除（改用鼠标拖拽定位）
- "清除头像"按钮更名为"恢复默认样式"（清除头像并回到实心圆）
- 预览 HUD 按钮移至「外观」页工具区

### 修复
- 头像缩放精确化：双线性重采样替代整数缩放，消除 200% 附近的尺寸突变
- READY 状态下头像样式不再消失（补画头像图）

## [v2.3] - 历史

- 键盘鼠标正式支持 + 手柄布局（Beta）
- 头像抓取（双击悬浮窗触发）
- 两种标识样式雏形（实心圆 / 头像）

## [v2.2]

- 键盘鼠标布局版本（由 v2.1.2 Beta 3 原样发布）

## [v2.1]

- 真实白条横向进度 → 圆形进度显示（display-only）
- 完整 CONSUMED → READY 周期学习，显示预计剩余秒数（滚动中位数，防加速期污染基线）
- 死亡双确认：低/无彩色 HP + 固定死亡图标相似度
- 中央三段技力条几何门控 HUD 显示（仅影响可见性，不影响状态机）
- READY 整圆高亮脉冲动画

## [v2.0.1]（稳定基线）

- 1~4 槽连携技 READY / CD 显示
- 连帧确认、READY 锁存、真实 CONSUMED 重装（防换人暗化误报）
- 前台自动隐藏、精确匹配 `Endfield.exe`、单实例运行
- 可配置的 HUD 外观（透明度、位置等）
